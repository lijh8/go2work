// golang.go extension can run and debug from the current main package.
// the println output shows on debug console pane.
// do not use launch.json and tasks.json,
// or it switch to terminal pane everytime, it is too annoying.

{
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "go",
            "args": ["build"],
            // "args": ["build", "-ldflags=-w -s"], // strip or omit the debug information
            // "args": ["build", "-gcflags=all=-N -l"], // disable optimizations
            "options": {
                // "cwd": "${workspaceFolder}/src",
                "cwd": "${fileDirname}",
            }
        }
    ],
    "version": "2.0.0",
}
